<%- include('../partials/header'); -%>
<div class="container">
    <h1 id="disp-form-name">comments form for <%= comment.author.username %></h1>
    <form class="form-group disp-form" action="/campgrounds/<%= campid%>/comments/<%= comment._id %>?_newmethod=PUT" method="POST">    
        <div class="form-group">
            <input class="form-control" type="text" name= newcomment[text] value="<%= comment.text %>">
        </div>
        <div class="form-group">
            <button class="btn btn-primary btn-block">submit</button>        
        </div>
    </form>
</div>
<%- include('../partials/footer'); -%>